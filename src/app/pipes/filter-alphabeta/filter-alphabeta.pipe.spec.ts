import { FilterAlphabetaPipe } from './filter-alphabeta.pipe';

describe('FilterAlphabetaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlphabetaPipe();
    expect(pipe).toBeTruthy();
  });
});
