import { FilterOfflinePipe } from './filter-offline.pipe';

describe('FilterOfflinePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOfflinePipe();
    expect(pipe).toBeTruthy();
  });
});
