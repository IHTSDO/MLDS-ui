import { FilterOnlinePipe } from './filter-online.pipe';

describe('FilterOnlinePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOnlinePipe();
    expect(pipe).toBeTruthy();
  });
});
