import { SortLimitPipe } from '../sort-limit.pipe';

describe('SortLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new SortLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
