import { LinkAddblinkPipe } from './link-addblink.pipe';

describe('LinkAddblinkPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkAddblinkPipe();
    expect(pipe).toBeTruthy();
  });
});
